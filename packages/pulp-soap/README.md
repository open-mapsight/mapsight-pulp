# Pulp SOAP

Extension for the Pulp library to handle SOAP requests as a source.

## Features

- **SOAP Source:** Fetch data from SOAP services and inject it into a Pulp stream.
- **Configurable Client:** Easily set up SOAP clients with WSDL and custom options.
